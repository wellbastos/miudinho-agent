apiVersion: v1
kind: Service
metadata:
  name: miudinho-agent
  namespace: o11y
spec:
  selector:
    app: miudinho-agent
  ports:
    - name: webhook
      port: 8090
      targetPort: 8090
    - name: probe
      port: 8080
      targetPort: 8080
